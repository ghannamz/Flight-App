server:
  port: 8080

spring:
  application:
    name: passenger-service

eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8085/eureka/
    instance:
      hostname: localhost